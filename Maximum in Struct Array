int findMax(Height arr[], int n)
{
    int sum = 0;
    int maxSum = 0;
    for(int i = 0; i  < n ; i++){
        sum = (arr[i].feet*12) + arr[i].inches;
        maxSum = max(maxSum, sum);
    }
    return maxSum;
}
