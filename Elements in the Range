  set<int> a;
	
		for(int i = 0; i < n; i++){
		    if(arr[i] >= A && arr[i] <= B){
		        a.insert(arr[i]);
		    }
		}
		 if(a.size() == abs(A-B)+1){
		        return true;
		    }
		    return false;
	}
